{
  "name": "knowledge-portal",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "generate": "node ./scripts/generate-vitepress-config.mjs",
    "docs:dev": "npm run generate && vitepress dev docs",
    "docs:build": "npm run generate && vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "devDependencies": {
    "gray-matter": "^4.0.3",
    "js-yaml": "^4.1.1",
    "vitepress": "^1.6.3",
    "vue": "^3.5.13"
  }
}
