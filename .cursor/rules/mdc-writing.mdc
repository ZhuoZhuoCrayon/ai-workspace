---
description: MDC 规则文档写作规范
globs: "**/*.mdc"
---

# MDC 写作规范

## 0x01 结构

- 使用 `## 0x01`、`## 0x02` 作为主章节编号。
- 子章节使用 `### a.`、`### b.` 格式。
- frontmatter 必须完整且合法，至少包含 `description` 与作用范围字段。

## 0x02 内容

- 规则说明写成可执行约束，不写空泛口号。
- 高优先级约束明确标记为 `CRITICAL（必须执行不可协商）`。
- 规则内容保持精炼，优先列“必须做什么”和“为什么”。
- 引用 document-style-guide 时，补充与当前工作区直接相关的具体要求，而不是只放链接。

## 0x03 格式

- 中英文之间加空格。
- 代码块必须标注语言。
- 文件末尾保留一个空行。
- 事项标题和总结性短语不用 `+`、`/` 拼接并列动作。
