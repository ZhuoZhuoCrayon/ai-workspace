---
description: Markdown 写作规范
globs: "**/*.{md,mdc}"
---

# Markdown 写作规范

## 0x01 结构

- 章节编号使用 `## 0x01`、`## 0x02` 格式。
- 子章节使用 `### a.`、`### b.` 格式。
- 标题层级不跳级。
- 一级标题下不要直接进入三级标题。
- 下级标题不要简单重复上级标题文字。
- `.mdc` 文件 frontmatter 必须完整且合法，至少包含 `description` 与作用范围字段。

## 0x02 格式

### a. 规范

**CRITICAL（必须执行不可协商）**：严格遵循 [document-style-guide（中文技术文档的写作规范）](https://github.com/ruanyf/document-style-guide)。

### b. 规则

- 中英文之间加空格。
- 数字使用半角字符。
- 代码块标注语言类型。
- 表格对齐。
- 文件末尾保留一个空行。
- 中文语境下优先使用全角标点。
- 省略号使用 `⋯⋯`，不要使用 `...`。

### c. 行文风格

- 一个列表项只承载一个动作或一条信息。
- 多步操作拆为有序子列表，禁止用分号串联多个命令。
- **表格单元格内禁止使用分号（`;`、`；`）串联多项内容**，两种替代做法：
  - 多行编号：`[1] xxx<br />[2] xxx`。
  - 角标引用：单元格内标记 `*[a]*`，表格外补充 `* *[a]* 详细说明`。
  - Good：`[1] Quota 计算字段用 field(init=False)<br />[2] register、__new__ 加注解`。
  - Bad：`Quota 计算字段用 field(init=False)；register、__new__ 加注解`。
- 表格外如果存在 2 项及以上并列信息，优先改成无序列表，不要用逗号、分号或长句硬串联。
  - Good：
    `结论调整概要：`
    `- 接口协议变更`
    `- 配置功能行为变更`
    `- 需验证全局规则下发与服务级优先级`
  - Bad：`结论调整概要：接口协议变更、配置功能行为变更、需验证全局规则下发与服务级优先级。`
- 表格补充说明不要写“注：”或“说明：”单独占一行，统一紧跟表格写无序列表脚注。
  - Good：`* *[1]* 检索规则与检索流程。`
  - Bad：`注：` 换行后再写 ``[1]`` 说明。
- 角标必须使用斜体写法：`*[1]*`、`*[a]*`。
- 角标只在提供额外信息时使用，如果角标只是重复表格行标题、用途列或同一行已有信息，则不要使用角标。
  - Good：表格外补充例外条件、适用边界、额外约束。
  - Bad：`检索 *[1]*`，而脚注只是在重复“检索场景使用”这类信息。
- **一句话内不出现两个句号（硬性规则）**，出现两个句号说明需要分拆为独立句子或段落。
  - Good：`mypy 按方法定义所在类解析属性类型，mixin 内看到的仍是 union 类型。`（逗号衔接同一主题）
  - Bad：`mypy 按方法定义所在类解析属性类型。mixin 内看到的仍是 union 类型。`（两个句号挤在一段内）
  - 改法：拆为两段、用逗号合并为一句，或改写为列表。
- **单行出现分号（`;`、`；`）说明需要拆分（硬性规则）**，分号通常意味着一行里塞了多个并列动作，应拆成分句、列表或换行。
  - Good：`先更新方案主干。再补实施进展。`
  - Good：`1. 先更新方案主干。 2. 再补实施进展。`
  - Bad：`先更新方案主干；再补实施进展。`
- **单行避免过长（硬性规则）**，正文单行建议不超过 `120` 个字符，超长通常说明需要换行、拆段或改成列表。
  - 例外：表格行、代码块和纯链接行不受此条检查。
- 代码标识符（类名、方法名、变量名、配置项等）用反引号包裹。
- 列表项 > 1 且字段固定时可用表格，否则优先无序列表。
- 无序列表项格式规整：`标识`：动作或说明，避免在标识前堆砌修饰词。
- 事项标题和总结性短语使用自然语言，不使用 `+`、`/` 等连接符拼接并列动作。
  - Bad：`数据库增加字段 + 序列化器重构（含标签国际化）`。
  - Good：`服务配置 DB 表变更`。
- 直击本质，避免凑字数，每句话必须传递有效信息。
- 注重逻辑连贯与文档整洁美感。
- 每段聚焦一个主题，过长段落拆分后再组织。
- 引用外部资料时给出来源或链接，避免孤立结论。

### d. 规则文档约束

- 规则说明写成可执行约束，不写空泛口号。
- 高优先级约束标记为 `CRITICAL（必须执行不可协商）`。
- 规则内容保持精炼，优先列"必须做什么"和"为什么"。
- 引用 document-style-guide 时，补充与当前工作区直接相关的具体要求，而不是只放链接。

### e. 技术文档定位

- 需求和方案文档面向 Agent 和人，是设计指引而非实现记录。
- 给出方向、约束和关键决策，留出探索空间，不贴完整代码。
- PLAN 中的开发方案：列变更清单和意图，不写具体实现。
- Issue 文档职责分离：`README.md` 只写需求与方法论，调研细节与结论（含对比表、快照、语法样例）必须写入 `PLAN.md`。

### f. 超链接文案规范

- 链接文本必须是摘要，不使用裸 URL 作为文案。
- 文案优先提取页面核心入口词，例如 `Quickstart`、`Usage`、`API`、`README`。
- 源码链接使用统一前缀：`<源码>` + 项目名 + 文件路径。
- 同一段落出现多个链接时，文案命名保持并列一致，避免一个是摘要、一个是 URL。
- 若无法提取明确摘要，再使用"项目名 + 页面类型"作为降级文案。

Good：

- `[limits Quickstart](https://limits.readthedocs.io/en/stable/quickstart.html)`
- `[<源码> limits limits/util.py](https://github.com/alisaifee/limits/blob/master/limits/util.py)`

Bad：

- `[https://limits.readthedocs.io/en/stable/quickstart.html](https://limits.readthedocs.io/en/stable/quickstart.html)`
- `[limits util.py](https://github.com/alisaifee/limits/blob/master/limits/util.py)`（缺少 `<源码>` 前缀）

## 0x03 文件命名

- 使用小写英文 + 短横线连接：`redis-connection-timeout.md`。
- 索引文件统一命名 `INDEX.md`。

## 0x04 适用范围与豁免

- 默认作用于工作区自有 `*.md` 和 `*.mdc` 文档。
- 第三方目录（如 `node_modules/`）中的 Markdown 不纳入写作规范要求。
