---
description: Markdown 写作规范
globs: "**/*.md"
---

# Markdown 写作规范

## 0x01 结构

- 章节编号使用 `## 0x01`、`## 0x02` 格式
- 子章节使用 `### a.`、`### b.` 格式
- 豁免：INDEX.md、AGENTS.md、CLAUDE.md 等结构性配置文件使用描述性标题即可，无需编号
- 标题层级不跳级；一级标题下不要直接进入三级标题
- 下级标题不要简单重复上级标题文字

## 0x02 格式

### a. 规范

**CRITICAL（必须执行不可协商）**：严格遵循 [document-style-guide（中文技术文档的写作规范）](https://github.com/ruanyf/document-style-guide)。

### b. 规则

- 中英文之间加空格
- 数字使用半角字符
- 代码块标注语言类型
- 表格对齐
- 文件末尾保留一个空行
- 中文语境下优先使用全角标点
- 省略号使用 `⋯⋯`，不要使用 `...`

### c. 行文风格

- 长句拆为短句，使用无序列表，每条以句号结尾。
- 代码标识符（类名、方法名、变量名、配置项等）用反引号包裹。
- 列表项 > 1 且字段固定时可用表格（更美观），否则优先无序列表。
- 无序列表项格式规整：`标识`：动作/说明。避免在标识前堆砌修饰词。
- 事项标题和总结性短语使用自然语言，不使用 `+`、`/` 等连接符拼接并列动作。
  - Bad：`数据库增加字段 + 序列化器重构（含标签国际化）`。
  - Good：`服务配置 DB 表变更`。
- 每段聚焦一个主题；过长段落拆分后再组织。
- 引用外部资料时给出来源或链接，避免孤立结论。

### d. 技术文档定位

- 需求和方案文档面向 Agent 和人，是设计指引而非实现记录。
- 给出方向、约束和关键决策，留出探索空间，不贴完整代码。
- PLAN 中的开发方案：列变更清单和意图，不写具体实现。

## 0x03 文件命名

- 使用小写英文 + 短横线连接：`redis-connection-timeout.md`
- 索引文件统一命名 `INDEX.md`

## 0x04 适用范围与豁免

- 默认作用于工作区自有 Markdown 文档。
- 第三方目录（如 `node_modules/`）中的 Markdown 不纳入写作规范要求。
- `.mdc` 文件使用单独规则：`.cursor/rules/mdc-writing.mdc`。
