---
description: Markdown 写作规范
globs: "**/*.{md,mdc}"
---

# Markdown 写作规范

## 0x01 总则

| 主题 | 规则 |
| --- | --- |
| frontmatter | `.mdc` 文件 frontmatter 必须完整且合法，至少包含 `description` 与作用范围字段。 |
| 规则表达 | [1] 规则说明写成可执行约束。<br />[2] 不写空泛口号。 |
| 高优先级 | 高优先级约束统一标记为 `CRITICAL（必须执行不可协商）`。 |
| 基础规范 | [1] 严格遵循 [document-style-guide（中文技术文档的写作规范）](https://github.com/ruanyf/document-style-guide)。<br />[2] 引用该规范时补充与当前工作区直接相关的具体要求。 |

## 0x02 结构规范

### a. 标题层级

| 主题 | 规则 |
| --- | --- |
| 编号 | [1] 章节编号使用 `## 0x01`、`## 0x02` 格式。<br />[2] 子章节使用 `### a.`、`### b.` 格式。 |
| 层级 | [1] 标题层级不跳级。<br />[2] 一级标题下不要直接进入三级标题。 |
| 命名 | [1] 标题只表达当前小节主题。<br />[2] 下级标题不要简单重复上级标题文字。<br />[3] 同级标题保持同一颗粒度。 |

### b. 方案文档骨架

方案类文档优先采用“主干结论先行，细化内容下沉”的结构。

| 文档类型 | 推荐骨架 |
| --- | --- |
| 方案类文档 | [1] 场景与约束。<br />[2] 方案主干。<br />[3] 文件级变更。<br />[4] 验证与回归。<br />[5] 风险与约束。<br />[6] 实施进展。 |
| 章节组织 | [1] 先给总思路与关键结论。<br />[2] 再按模型、查询、写入、下发、风险等子段展开。<br />[3] 同类信息优先落在固定小节，不在不同章节重复展开。 |

Bad：

```md
## 0x01 背景、问题、方案、改造、验证说明
```

Good：

```md
## 0x01 场景与约束
## 0x02 方案主干
## 0x03 文件级变更
## 0x04 验证与回归
## 0x05 实施进展
```

## 0x03 行文规范

| 主题 | 规则 |
| --- | --- |
| 结论表达 | [1] 结论先行，先写判断，再补必要说明。<br />[2] 结论写成可判定、可复用、可验证的句子。 |
| 句子 | [1] 每句话只表达一个信息点。<br />[2] 超过两层逻辑就拆句。 |
| 段落 | [1] 每段只聚焦一个主题。<br />[2] 单段不超过 `3` 句。<br />[3] 超过则拆段或改写为列表。 |
| 段内硬规则 | [1] 同一 Markdown 段落或列表项内不出现两个句号。<br />[2] 同一 Markdown 段落或列表项内不出现分号。<br />[3] 规则以 Markdown 渲染后的段落或列表项为准。<br />[4] 软换行不构成规避例外。 |
| 长度 | [1] 单句内容若合并后不超过 `120` 个字符，必须写成一行。<br />[2] 正文单行建议不超过 `120` 个字符。<br />[3] 表格行、代码块和纯链接行不受单行长度规则限制。 |
| 信息密度 | [1] 直击本质，避免凑字数。<br />[2] 每句话都应传递有效信息。 |
| 风格禁忌 | [1] 避免“进一步优化”“更合理”“增强体验”等空泛词。<br />[2] 避免“简单来说”“这里先说明一下”之类的口语化和自述式表达。 |
| 可扫描性 | [1] 解释类内容优先用列表、编号或表格。<br />[2] 目标是让读者在 `10` 秒内抓住要点。 |

Bad：

```md
这一部分我们主要讨论一下为什么这个方案值得考虑，然后再看具体实现。
```

Good：

```md
结论：采用方案 A。

原因：实现成本最低。
```

Bad：

```md
应用数据源（ApmDataSourceConfigBase）通过 `shared_datasource_id`
引用共享池。
```

Good：

```md
应用数据源（ApmDataSourceConfigBase）通过 `shared_datasource_id` 引用共享池。
```

## 0x04 结构化承载

### a. 列表与表格

| 载体 | 规则 |
| --- | --- |
| 无序列表 | [1] 每个列表项只承载一条信息。<br />[2] 适合表达并列结论、约束和验证点。 |
| 有序列表 | [1] 多步操作拆为有序列表。<br />[2] 不用分号串联多个动作。 |
| 表格适用场景 | [1] 字段固定且项数超过 `1` 时优先用表格。<br />[2] 方案比较、规则条件、风险分级、PR 拆分、测试节奏优先用表格。 |
| 多文件改造 | [1] 改动文件超过 `1` 个时，优先用表格归属。<br />[2] 推荐列为 `文件`、`场景`、`变更点`、`说明`。 |

### b. 表格单元格

| 主题 | 规则 |
| --- | --- |
| 单元格内容 | [1] 表格单元格内禁止使用分号（`;`、`；`）串联多项内容。<br />[2] 多项内容优先用 `[1] xxx<br />[2] xxx`。 |
| 角标 | [1] 需要额外说明时可使用 `*[1]*`、`*[a]*`。<br />[2] 角标必须使用斜体写法。<br />[3] 角标只补充额外信息，不重复行标题或用途列。 |
| 表格外补充 | [1] 并列信息超过 `2` 项时优先拆成列表。<br />[2] 表格补充说明不要单独写“注：”或“说明：”。<br />[3] 统一紧跟表格写无序列表脚注。 |

Bad：

```md
- apm_web.service.resources 新增 ServiceListResource，apm_web.service.views 新增 ResourceRoute，整体影响前端与 MCP。
```

Good：

```md
| 文件 | 场景 | 变更点 | 说明 |
| --- | --- | --- | --- |
| `apm_web.service.resources` | 查询 | `ServiceListResource` | 提供统一服务列表入口 |
| `apm_web.service.views` | 路由 | `ResourceRoute` | 暴露新接口 |
```

## 0x05 技术方案文档

### a. 文档职责

| 文档 / 章节 | 回答问题 | 应写内容 | 不写内容 |
| --- | --- | --- | --- |
| `README.md` | 需求是什么 | [1] 需求定义。<br />[2] 方法论。<br />[3] 调研对象列表。 | [1] 调研细节。<br />[2] 对比结论。<br />[3] 语法样例结论。 |
| 实现方案 | 做什么 | [1] 架构。<br />[2] 协议。<br />[3] 边界。<br />[4] 验收。<br />[5] 风险。 | [1] 代码级细节。<br />[2] 逐文件实现步骤。 |
| 开发方案 | 怎么做 | [1] 代码落点。<br />[2] 字段与接口改造。<br />[3] 数据结构。<br />[4] 迁移步骤。<br />[5] 测试点。 | [1] 重新讨论需求目标。<br />[2] 重复解释实现方案中的设计语义。 |

### b. 方案主干写法

| 主题 | 规则 |
| --- | --- |
| 场景先行 | [1] 接口与协议描述先写场景。<br />[2] 再写输入、输出和约束。 |
| 视角拆分 | [1] 涉及不同视角时，先分“应用视角”“服务视角”等场景。<br />[2] 不要直接写“这个传”“这个不传”。 |
| 规则拆分 | [1] 模型、查询、写入、下发、风险等不同规则类型分小节表达。<br />[2] 不把背景、方案、验证、进展混在同一段。 |
| 关键结论 | [1] 明确默认值、唯一键、优先级、兼容策略、迁移策略。<br />[2] 避免只写“支持”“优化”“增强”这类方向词。 |
| 职责命名 | [1] 用“实现方案”“开发方案”等职责名表达文档分工。<br />[2] 不要用 `0x01`、`0x02` 这类章节号代替语义。 |

Bad：

```md
service_name 不传：返回 remarks 数组。

service_name + kind：返回兼容的 {code: remark}。
```

Good：

```md
[1] 应用视角：`service_name` 不传，返回 `remarks` 数组。
[2] 服务视角：兼容原有参数与接口行为。
```

Bad：

```md
实现方案：

- 改 `fooService.handle()` 的第 3 个分支。
- 新增 `bar_id` 字段。
```

Good：

```md
实现方案：

- 支持按 `bar` 维度处理。
- 验收标准是返回结果包含 `bar` 维度统计。

开发方案：

- 在 `fooService.handle()` 增加 `bar` 分支。
- 为输出结构补充 `bar_id` 字段。
```

## 0x06 链接与格式细则

| 主题 | 规则 |
| --- | --- |
| 链接文案 | [1] 链接文本必须是摘要。<br />[2] 不使用裸 URL 作为文案。 |
| 链接命名 | [1] 文案优先提取页面核心入口词，例如 `Quickstart`、`Usage`、`API`、`README`。<br />[2] 同一段落出现多个链接时，文案命名保持并列一致。 |
| 源码链接 | 源码链接使用统一前缀：`<源码>` + 项目名 + 文件路径。 |
| 降级策略 | 若无法提取明确摘要，再使用“项目名 + 页面类型”作为降级文案。 |
| 引用 | 引用外部资料时给出来源或链接，避免孤立结论。 |
| 微观格式 | [1] 中英文之间加空格。<br />[2] 数字使用半角字符。<br />[3] 中文语境下优先使用全角标点。<br />[4] 省略号使用 `⋯⋯`。<br />[5] 代码标识符用反引号包裹。<br />[6] 代码块标注语言类型。 |

Good：

- `[limits Quickstart](https://limits.readthedocs.io/en/stable/quickstart.html)`
- `[<源码> limits limits/util.py](https://github.com/alisaifee/limits/blob/master/limits/util.py)`

Bad：

- `[https://limits.readthedocs.io/en/stable/quickstart.html](https://limits.readthedocs.io/en/stable/quickstart.html)`
- `[limits util.py](https://github.com/alisaifee/limits/blob/master/limits/util.py)`

## 0x07 文件级约束

| 主题 | 规则 |
| --- | --- |
| 文件命名 | 使用小写英文加短横线命名，例如 `redis-connection-timeout.md`。 |
| 索引文件 | 索引文件统一命名为 `INDEX.md`。 |
| 文件尾 | 文件末尾保留一个空行。 |

## 0x08 适用范围与豁免

| 主题 | 规则 |
| --- | --- |
| 默认作用范围 | 默认作用于工作区自有 `*.md` 和 `*.mdc` 文档。 |
| 豁免范围 | 第三方目录，例如 `node_modules/`，中的 Markdown 不纳入写作规范要求。 |
