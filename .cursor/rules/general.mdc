---
description: 通用编码和交互规范
alwaysApply: true
---

# 通用规范

## 0x01 语言

- 文档和沟通使用中文
- 代码注释使用英文
- 变量命名使用英文

## 0x02 格式

- 所有文档使用 Markdown
- 引用文件和目录时使用完整相对路径
- 路径格式示例：`knowledge/<project>/snippets/xxx.md`
- Issue 目录仅保留 `README.md` 与 `PLAN.md`
- 进展记录合并写入 `PLAN.md`，不创建 `PROGRESS.md`

## 0x03 输出

- 回答简洁直接，避免冗余
- 优先给出可执行的方案
- 涉及工作区知识目录检索或知识管理时，优先检查 `.agents/skills/knowledge_mgr/SKILL.md`，再按需进入同目录 `references/`
- 涉及工作区操作时，先检查相关 `.agents/skills/` 下的 Skill
- 涉及 Markdown 文档编写、重构、润色或结构化整理时，优先检查 `.agents/skills/doc-style/SKILL.md`，再按需进入其 `references/`
- 文档检查默认执行三轨：`lint-md`（中文文案规范）+ `markdownlint-cli2`（Markdown 结构）+ `scripts/check_doc_style.py`（仓库定制规则）
