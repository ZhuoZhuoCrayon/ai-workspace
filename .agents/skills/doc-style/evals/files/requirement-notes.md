# 需求草稿

- 想把配置治理的需求整理成 README。
- 目标是让服务侧、应用侧和全局兜底的边界清楚。
- README 只想放目标、范围、非目标、调研方法和验收标准。
- 具体协议、数据模型、迁移步骤和文件级改造放到 PLAN。
- 需求范围包括统一默认配置、保留服务侧显式配置、提供可追溯的查询结果。
- 非目标包括本期不重做权限体系，不改旧协议字段，不展开历史数据清洗细节。
- 验收要能证明旧调用方不受影响，也能证明新入口可追溯。
