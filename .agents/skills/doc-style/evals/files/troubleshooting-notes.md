# 排障笔记

- 现象：导出接口偶发返回 500。
- 用户刷新后有时恢复。
- 日志里有 `KeyError: total_count`。
- 也有少量 `ZeroDivisionError`。
- 先看了接口参数，发现都集中在最近 5 分钟窗口。
- 再查聚合日志，发现部分 bucket 缺 `total_count` 字段。
- 继续看代码，发现 Web 层没有过滤不完整 bucket。
- 修复思路是先在聚合结果出口过滤不完整 bucket，再给 Web 层补默认值兜底。
- 回归时要覆盖热窗口、冷窗口、空结果和历史正常数据。
