# 标题与结构

## 0x01 工作区编号约定

- 常规结构：`## 0x01 标题`、`### a. 标题`。
- 复杂内容允许 `#### 标题`：但优先用项目列表 `**（1）A**` 承载并列子项。

## 0x02 标题原则

- 标题层级连续使用不跳级：一级标题下不能直接出现三级标题。
- 标题避免孤立编号：同级标题只有一个时省略该层级。
- 下级标题不重复上级标题名字。
- 同级标题保持同一颗粒度：只表达当前小节主题，不附加 `*[1]～[n]*` 等正文索引。
